micronaut:
  application:
    name: genre-demo
datasources:
  default:
    driver-class-name: com.mysql.cj.jdbc.Driver
    db-type: mysql
    dialect: MYSQL
flyway:
  datasources:
    default:
      enabled: true
